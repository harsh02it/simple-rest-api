pipeline {
    agent any

    tools {
        nodejs('NodeJS')
    }

    environment {
        SCANNER_HOME = tool 'SonarQubeScanner'
        NEXUS_VERSION = "nexus3"
        NEXUS_PROTOCOL = "http"
        NODEJS_HOME = tool name: 'NodeJS'
        PATH = "${NODEJS_HOME}/bin:${env.PATH}"
        SONARQUBE_SERVER = 'SonarQube' 
        NEXUS_URL = 'localhost:8081'
        NEXUS_REPO = 'npm-task-repo'
        NEXUS_CREDENTIALS_ID = 'npm-nexus-creds'
    }


    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                bat 'npm install'
            }
        }

        stage('Build') {
            steps {
                withCredentials([file(credentialsId: 'npm-nexus-creds', variable: 'mynpmrc')]) {
                    echo 'Building...'
                    bat 'npm install --userconfig $mynpmrc --registry http://localhost:8081/repository/npm-task-repo --loglevel verbose'
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                script {
                    withSonarQubeEnv('SonarQube') {
                        bat """${SCANNER_HOME}/bin/sonar-scanner -Dsonar.host.url=http://localhost:9000/ \
                        -Dsonar.token=squ_aa488066755e2e65b859a3f26fa83559e3779157 \
                        -Dsonar.projectName="simple-rest-api" \
                        -Dsonar.exclusions=**/node_modules/** \
                        -Dsonar.projectKey=simple-rest-api"""
                    }
                }
            }
        }

        stage('Quality Gate') {
            steps {
                script {
                    // Wait for Quality Gate and get the result
                    def qualityGate = waitForQualityGate()
                    
                    // Check the Quality Gate result
                    if (qualityGate.status == 'OK') {
                        echo "Quality Gate passed: ${qualityGate.status}"
                    } else if (qualityGate.status == 'IN_PROGRESS') {
                        error "Unexpected status: Quality Gate is still in progress. Check SonarQube settings."
                    } else {
                        error "Quality Gate failed: ${qualityGate.status}"
                    }
                }
            }
        }



        stage('Publish to Nexus') {
            steps {
                withCredentials([file(credentialsId: 'npm-nexus-creds', variable: 'mynpmrc')]) {
                    echo 'Publishing to Nexus...'
                    bat 'npm publish --userconfig $mynpmrc --loglevel verbose'
                }
            }
        }
    }

    post {
        success {
            echo 'Pipeline completed successfully!'
        }
        failure {
            echo 'Pipeline failed.'
        }
    }
}
